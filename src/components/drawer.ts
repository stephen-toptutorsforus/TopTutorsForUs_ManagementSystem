/**
 * The id the mobile drawer opens on.
 *
 * Shared because it is the whole mechanism: the header links to it, the
 * sidebar answers to it, and the stylesheet matches `:target` against it. Three
 * places, one string — spelling it three times is how a drawer stops opening.
 */
export const DRAWER_ID = "primary-nav";
